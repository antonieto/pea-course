#include <iostream> 
#include <vector> 
#include <bits/stdc++.h> 

using namespace std;

// vector<int> bubble_sort(vector<int> A){ 

//     return vector<>
// }


int main() { 
    int n = 10;

    // Set random seed 
    srand(time(0));

    vector<int> x(n, 0); 

    for(int i = 0; i < n; i++){ 
        cout << x[i] << endl; 
    }

    return 0;
}